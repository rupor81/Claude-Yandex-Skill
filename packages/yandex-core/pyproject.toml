[project]
name = "yandex-core"
version = "0.1.0"
description = "Shared contracts for the Yandex MCP connectors."
requires-python = ">=3.13"
dependencies = ["mcp>=2", "keyring>=25", "pydantic>=2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
