---
topic: Yandex MCP connectors for Claude (Disk, Mail, Calendar)
updated: 2026-08-27T10:56
---

- (decision) Transport: local stdio MCP servers now, core decoupled from transport to allow a remote HTTP+OAuth connector later
- (decision) Permission scope: full access including SMTP send and delete, but destructive ops flagged, requiring explicit confirmation and supporting dry-run
- (decision) Stack: Python monorepo - shared yandex_core (profiles, OAuth, secret storage, errors, confirmation policy) plus three independent MCP servers
- (decision) Accounts: support both personal Yandex ID and Yandex 360 domain accounts via switchable profiles
- (event) Constraint verified from Yandex docs: no unified REST API - Disk is REST+OAuth, Mail is IMAP/SMTP+XOAUTH2 only, Calendar is CalDAV requiring an app password (OAuth not accepted)
- (decision) BMAD languages: conversation Russian, documents English; pinned in _bmad/custom overrides so the installer cannot clobber them
- (event) GitHub remote attached: https://github.com/rupor81/Claude-Yandex-Skill.git (currently empty)
- (decision) Working mode: coaching path chosen for the brief
- (decision) Flagship scenario identified: reconstruct project history by joining Calendar meetings, emailed meeting notes, and Disk artifacts on a project key - the only scenario requiring all three connectors together
- (event) User confirmed naming discipline: all calendar meetings carry the project name in the title, so the project key is derivable from meeting titles
- (event) CONFIRMED from official Yandex docs: Disk REST /v1/disk/resources/files supports only limit, offset, media_type, fields, preview params - no name or full-text search of any kind, flat alphabetical list only
- (event) REPORTED but unverified: Yandex IMAP SEARCH is unreliable with Cyrillic when combining FROM and SUBJECT criteria - needs empirical check against a real mailbox
- (decision) Consequence: a local metadata index with incremental sync becomes a fourth core component alongside the three connectors, since no Yandex service offers usable server-side search
- (assumption) Local index keyed by project is assumed to serve both the flagship scenario and the Disk cleanup case - to be validated against real data volumes
- (decision) Revised earlier position: the local index is NOT required by the flagship scenario - tracing the query showed date-anchored retrieval suffices. Index scoped to Phase 3 (Disk) only
- (decision) Phasing set: P1 Calendar+Mail read paths for the flagship scenario, P2 write paths with confirmation and dry-run, P3 Disk inventory, index and sorting
- (decision) Calendar acts as the index: meeting titles carry the project key and dates are the only reliable filter in both CalDAV and IMAP
- (assumption) Meeting-note format (sender, subject content, body vs link) is unknown; recorded as open question 1 - affects precision of step 5 only, not the chain structure
- (event) Brief and addendum drafted at _bmad-output/planning-artifacts/briefs/brief-Yandex-Skills-2026-08-26
- (override) REVERSAL: the deliverable is the connector set itself, not the cross-service scenario. User corrected the framing - scenarios are composed by Claude at runtime and serve as acceptance tests. Removing project-key joins, cross-service logic and the local index from scope
- (decision) Simplify safety: rely on MCP tool annotations (readOnlyHint, destructiveHint) plus the client's own per-call approval instead of a bespoke confirmation layer. Keep dry_run only on bulk operations
- (decision) Quality bar renamed and made explicit: correct and quiet operation - predictable output size, honest errors, no context flooding, no surprises
- (decision) Phasing reorganised by connector rather than by scenario
