---
topic: Yandex MCP Connectors for Claude - PRD
updated: 2026-08-27T11:08
---

- (event) Inputs: brief.md, addendum.md and .memlog.md (23 entries) from brief-Yandex-Skills-2026-08-26
- (decision) Frame carried from brief and not reopened: the connector set is the deliverable; scenarios are composed by Claude at runtime and act as acceptance tests. No cross-service logic, project key, local index or semantic search in scope
- (decision) Coaching path, Vision+Features entry point. Stakes: personal instrumentation - target roughly two pages, no market/monetisation/product-metrics sections
- (decision) Safety model: MCP readOnlyHint/destructiveHint annotations plus client-side approval; dry_run only on bulk operations
- (event) Sizing answered: Disk under ~1000 files, mailbox spans several years, calendar meetings are mostly recurring; limits and pagination required throughout
- (decision) Disk enumeration risk retired: at under 1000 files the whole disk lists in a single limit=1000 request, so name search is a complete local filter rather than a costly crawl
- (decision) Recurring-heavy calendar makes RRULE expansion a first-phase requirement, not an edge case
- (decision) event_update and event_delete take a mandatory scope parameter (occurrence|series) with no default, so the model cannot silently destroy a whole series. this-and-following deferred
- (decision) Mail date-ranged tools require an explicit window; no all-history default. Cursor pagination over UIDs
- (assumption) Message bodies default to text, truncated at a character limit with an explicit truncation marker; quote stripping offered as an optional parameter. User did not state a preference
- (event) PRD and addendum drafted: 26 tools across three servers, 8 cross-cutting NFRs, 3 open items
- (decision) Deletion is always to Trash on both Disk and Mail; no permanent delete tool exists, keeping every destructive operation user-recoverable
- (decision) Excluded on purpose: Disk public share links, server-side mail filters, calendar sharing and delegation, permanent deletion
